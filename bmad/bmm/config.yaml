# BMM Module Configuration
# Generated by BMAD installer
# Version: 6.0.0-alpha.3
# Date: 2025-10-30T19:14:45.166Z

project_name: claude-code-project-index
include_game_planning: false
user_skill_level: expert
tech_docs: '{project-root}/docs'
dev_story_location: '{project-root}/docs/stories'
tea_use_mcp_enhancements: true

# Core Configuration Values
user_name: Ryan
communication_language: English
document_output_language: English
output_folder: '{project-root}/docs'

# Relevance Scoring Configuration (Story 2.7)
relevance_scoring:
  enabled: true
  top_n: 5  # Load top 5 modules by default
  weights:
    explicit_file_ref: 10.0  # Highest priority (10x baseline)
    temporal_recent: 1.0     # Multiplied by recency factor (5x for 7-day, 2x for 30-day)
    semantic_keyword: 1.0    # Baseline weight for keyword matching
  temporal_windows:
    recent_7d: 5.0   # 0-7 days: 5x multiplier
    medium_30d: 2.0  # 8-30 days: 2x multiplier
    older_90d: 1.0   # 31-90 days: 1x multiplier

# Impact Analysis Configuration (Story 2.8)
impact_analysis:
  enabled: true
  max_depth: 10           # Maximum traversal depth for indirect callers
  include_indirect: true  # Include indirect callers in report (vs direct only)
  show_line_numbers: true # Include file:line information in reports